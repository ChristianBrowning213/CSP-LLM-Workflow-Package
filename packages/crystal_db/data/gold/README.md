# Gold Dataset (Synthetic)

This folder contains a tiny synthetic CIF set used for regression tests of CrystalCard semantics.
The CIFs are intentionally minimal and serve as deterministic fixtures. Replace with locally
provided public-domain CIFs if you need real structures, and update `gold_expectations.json`
accordingly.
