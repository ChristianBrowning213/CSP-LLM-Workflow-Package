# Crystal-DB

Portable SQLite-backed crystal retrieval and CSP-evidence packaging for the
LLM-CSP workflow. The base install is standard-library only. A populated,
compatible database/index is external runtime data and is never bundled.

See the repository's `docs/crystal_db.md` for configuration and embedding-space
compatibility requirements.
