# Retrieval Modes

`retrieval.result.v2` supports:

- `metadata`
- `text`
- `fingerprint`
- `hybrid`

Hybrid fusion records `fusion_notes` with explicit weights, and every selected item
retains provenance and a `why_returned` explanation.
