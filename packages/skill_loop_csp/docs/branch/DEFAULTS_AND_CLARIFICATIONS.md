# Defaults and Clarifications

## Hard Required
- composition target
- solve mode

## Important but Defaultable
- symmetry preference and hardness
- retrieval strictness
- iteration budget

## Optional
- motif preference
- property bias request

Non-critical missing fields proceed with explicit `defaults_used` entries in `task_spec.v1`.
