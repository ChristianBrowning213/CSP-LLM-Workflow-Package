# Cell Selection Method

Supported selectors:

- `baseline_default`: deterministic default candidate list.
- `retrieval_informed`: baseline defaults plus retrieval-derived cell hints.
- `fixed_benchmark`: fixed case-provided candidates for reproducible benchmarks.

All paired baseline/guided runs for one case must use the same cell candidate set.
