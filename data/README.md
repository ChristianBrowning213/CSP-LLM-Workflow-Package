# Data policy

This directory is limited to documentation and tiny, legally redistributable demo or test assets under `demo/`.

Do not commit the full Crystal-DB, downloaded Materials Project corpora, generated embeddings, large CIF collections, benchmark datasets, or generated experiment outputs. Large and generated data must remain outside Git and be configured through supported mechanisms introduced by later migration tickets.

