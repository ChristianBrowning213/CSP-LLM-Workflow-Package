# v0.1.0 release-candidate verification

Verification date: 2026-09-12

- Candidate commit: the commit containing this finalized report
- Definitive build source: `471db709f80b4ce89fa2b94d2a780a008de78c0f`
- Build host: Windows, Python 3.12.10
- Build command: `python -m build`
- Build isolation: enabled; clean no-local Git checkout

## Release versions

| Distribution | Version |
| --- | --- |
| llm-csp | 0.1.0 |
| crystal-db | 0.1.0 |
| qlip | 0.1.0 |
| SCA | 0.1.1 at `3ede1ee2ad1a972b7c0a0809a9ec7bdab9b1b6af` |

Exact filenames, sizes, and SHA-256 values are recorded in
`v0.1.0-artifacts.txt`. All six public wheel/sdist artifacts were inspected
independently. Required modules, schemas, policy resources, licences, and
metadata were present. Blocked POTs and hashes, legacy/generated data tables,
databases, CIF corpora, models, caches, Git data, and developer artifacts were
absent. No archive entry exceeded 1 MiB.

## Installation and API gates

The core artifact environment installed llm-csp, Crystal-DB, and QLIP solely
from candidate wheels. All documented public imports passed from
`site-packages`; `sys.path` contained no source or sibling repository. SCA was
absent, `llm_csp.validation` imported, and validation returned
`backend_unavailable`.

A second clean environment installed all three source distributions and
reported the same 0.1.0 versions and `site-packages` paths; the CLI version
gate also passed there.

`llm-csp --version` printed `llm-csp 0.1.0`. Help passed, and the offline demo
exited successfully. It used a generated fixture, made no network request, ran
no optimization, used no scientific POT, and returned no crystal prediction.

Installing the root wheel's `validation` extra installed SCA 0.1.1 from the
pinned commit. Synthetic NaCl general validation and ROCKSALT topology passed;
direct SCA records/metrics and adapter values were exactly equal.

## Failure and reproducibility gates

- Missing POT root: explicit `ValueError` identifying the unavailable source
  POT directory, before QLIP execution.
- Missing Crystal-DB database: structured `missing_db`; no empty database was
  created and no embedding fallback was selected.
- Embedding incompatibility: dimension mismatch is rejected before similarity
  calculation by the passing public regression.
- Gurobi: the preflight regression preserves structured
  `gurobi_unavailable`; production solving requires the runtime and a usable
  licence. No alternate solver was introduced.
- Chemistry tables: complete Windows-canonical SHA-256 parity passed for all
  118 element records, 118 radius records, and 486 ionic-radius rows.

## Test results

| Tier | Result |
| --- | --- |
| Unit | 130 passed |
| Integration, core/no SCA | 33 passed, 6 skipped |
| Public E2E | 3 passed |
| Core whole repository | 166 passed, 6 skipped |
| Validation tier | 10 passed, 1 skipped |
| Validation-enabled whole repository | 170 passed, 5 skipped |
| External scientific optional | 3 skipped when `LLM_CSP_EXTERNAL_POT_ROOT` was absent |

The remaining validation-enabled skips identify external requirements: a
NASICON scaffold corpus, a lawful SrTiO3 POT root for three scientific tests,
and a verified strictly read-only production Crystal-DB integration path.

## Distribution scan

The committed-candidate scan covered 229 tracked/new release files. It found no
file over 1 MiB, unexpected database/POT/model artifact, active developer
Windows path, private-key material, assigned real credential, or blocked POT
content. The only credential-like values are the documented Crystal-DB API-key
environment variable and its non-secret local LM Studio placeholder. Blocked
POT hashes and historical source paths occur only in clearly archival
`docs/packaging` evidence.

## Licensing and external requirements

`LICENSING_RESOLVED`

Production operation still requires a production Crystal-DB corpus/index,
compatible embedding backend, lawfully sourced POT library, finite QLIP design
space, Gurobi runtime/licence, and optional third-party ML validation assets
when requested.

## Known limitations and recommendation

No production corpus or broad POT library is bundled. Real QLIP runs require a
finite design space and Gurobi. Validation beyond base SCA may require external
ML assets. The deterministic workflow is included; agentic orchestration is
not.

`READY_WITH_DOCUMENTED_EXTERNAL_REQUIREMENTS`
